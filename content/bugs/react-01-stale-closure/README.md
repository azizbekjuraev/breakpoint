# Page title never updates when count changes

The component is supposed to update `document.title` to reflect the current count. The visible "Count: N" in the page updates fine when you click `+`, but the page title in the browser tab stays stuck at `Count: 0`.

**Before you run it: read the `useEffect`. What is its dependency array telling React?**
