# Typing in the input does nothing

The input is supposed to let you type a name and show a greeting. Every keystroke is ignored — the value stays empty no matter what you type.

**Before you run it: look at the `<input>`. It has a `value` prop. What other prop is missing?**
