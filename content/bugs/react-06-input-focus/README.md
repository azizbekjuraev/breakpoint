# Input loses focus after every character

Try typing in the input. After every single keystroke, the cursor jumps out — you have to click back in to type the next character. The state still updates, but the user experience is unusable.

**Before you run it: where is the `Input` component defined? What happens to its function reference each time `App` renders?**
