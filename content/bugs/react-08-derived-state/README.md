# Total never updates when items change

The cart shows item prices and a total. Click _Add Cherry_ — Cherry appears in the list (good), but the total stays stuck at `$150` instead of becoming `$225`.

**Before you run it: where does `total` come from? What updates it when items changes?**
