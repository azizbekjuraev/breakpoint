# Long list is cut off with no way to scroll

A fixed-height card contains a list of 12 entries, but only the first few are visible. The rest are clipped — there's no scrollbar, and you can't see the rest of the items at all.

The `height` and `overflow` declarations are both already there. The intent is right; the value is wrong.

**Before you run it: what do you think will happen, and why?**
