# Form submit clears the page

Type your name and click _Submit_. The page flashes and reloads — your input is gone, and the _Submitted: Alice_ message you expected never appears.

**Before you run it: what does a `<form>` do by default when it's submitted? Does React's `onSubmit` alone change that?**
