# Form submit clears the page

Type your name and click *Submit*. The page flashes and reloads — your input is gone, and the *Submitted: Alice* message you expected never appears.

**Before you run it: what does a `<form>` do by default when it's submitted? Does React's `onSubmit` alone change that?**
