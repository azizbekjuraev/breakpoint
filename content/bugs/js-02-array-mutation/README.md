# Adding to a list mutates the original

`addItem(list, item)` should return a _new_ list with the item appended, leaving the original untouched. Instead, the original gets modified too — and `updated` and `original` end up being the same array.

**Before you run it: what do you expect each line to print, and why?**
