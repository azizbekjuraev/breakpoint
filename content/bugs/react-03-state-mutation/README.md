# Adding an item to the list does nothing

Click _Add Cherry_ and… nothing happens. No new item appears in the list. The handler runs, but the UI doesn't update.

**Before you run it: look at `addCherry`. What does `items.push('Cherry')` return, and what does `setItems(items)` pass to React?**
