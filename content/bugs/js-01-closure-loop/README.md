# All timeouts log the same number

The loop below should log `1`, `2`, `3`, `4`, `5` — one per `setTimeout`.

Instead, it logs `6` five times.

**Before you run it: what do you think will happen, and why?** Form a prediction _first_, then click _Run tests_.
